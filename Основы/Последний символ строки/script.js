"use strict";

// Последний символ можно найти с помощью длины строки
let str = 'abcde';
let last = str[str.length - 1];
console.log(last);

// Задача 2
let predLast = str[str.length - 2];
console.log(predLast);

// Задача 3
let predPredLast = str[str.length - 3];
console.log(predPredLast);